language: go
install: go get -v github.com/spf13/hugo
script:
  - hugo --theme bluejam
  - python --version
  - sudo pip install s3cmd
  - sudo pip install pygments
  - s3cmd sync --delete-removed -P --no-mime-magic -M -r public/ s3://jre.id.au/
notifications:
    email:
on_failure: always
